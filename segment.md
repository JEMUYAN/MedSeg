开发需求：接收待测图片以及 RAG 库的 Top-k 检索结果，通过 Sam3 完成分割任务

- 输入：待测图片文件、RAG 库的 Top-k 检索结果
- 输出：分割后的图像文件
- 处理流程
  - 特征提取
    - 输入图像编码：将输入的原始图像输入到图像编码器中提取特征
    - 检索图像编码：将 RAG 库的 Top-k 检索结果中的图像文件输入到图像编码器中提取特征
  - 存入 memory bank
    - 检索到的图像特征及其对应的专家标注掩码被一同输入到记忆编码器中
    - 将记忆编码器的输出存入 memory bank
  - 按照 Sam3 的 pipeline 完成分割任务
  - 图像编码器、记忆编码器和memory bank都是 Sam3 中的组件
- Sam3 细节解释
  - Sam 在处理图片的时候不会启用记忆注意力机制，为了利用这个机制，我们需要将输入图片包装成只有一帧的视频流
  - 需要借助官方提供的 api（如果有的话）或者强行更改源码中的状态，来达到修改 memory bank 的目的
- 环境配置
  - 使用 conda 配置环境
  - 如果可以与 RAG 系统的环境配置一致，就不需要重新配置环境
  - 如果需要重新配置环境，需要安装 Sam3 所需要的依赖
  - 必须输出配置环境的 requirement.txt
- Sam3
  - 优先使用 huggingface 的 transformers 库调用 Sam3 模型，如果该方法无法满足开发需求，允许使用本地权重搭配源码进行调用（权重由开发者配置，你指定权重路径即可）

