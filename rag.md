开发需求：利用 dinov3 作为 embedding 层，FAISS 作为检索模块实现 RAG 系统
- 输入：图像文件
- 输出：检索的 top-K 结果
- 检索模块存储内容：图像文件的 embedding 向量
- 新建 git 仓库，用于存储代码。为本次开发创建一个新的分支，命名为 rag


- 完整的索引生成流程
    - 利用 dinov3 模型输出的 cls token 提取图像的 embedding 向量
    - dinov3 的嵌入向量需要进行归一化处理
    - 利用 FAISS 模型实现检索，采用 L2 距离作为检索指标

- 检索库细节
    - 被查找的文件是图像和独立的 mask 文件
    - 掩码不是检索的对象，生成索引时只依靠图像
    - 根据 RAG 系统产生的检索结果，要返回对应的图像和掩码
    - 检索库的文件数量和大小不大，只需要简单实现


- RAG 系统的封装使用
    - 不需要对 RAG 系统进行封装，直接调用即可
    - 调用时只需要输入图像文件，即可返回检索结果
    - 为增删检索库的文件提供简单的接口
    - RAG 系统是上层系统的一个子模块，需要在上层系统中调用

- 环境配置
    - 使用conda配置环境
    - 输出 requirment.txt，不需要你手动安装依赖
    - 分析所需要的依赖，如果可以再一个环境中安装，就不需要再其他环境中安装
    - 如果需要多个环境，分别编写 requirement.txt 文件，环境用模块自己的名称命名（比如 dinov3 的环境命名为 dinov3）
    - 每个环境的 requirement.txt 中，只包含该环境所需要的依赖
    - 如果涉及多个环境，在封装rag库时需要考虑跨环境调用的问题，可以使用环境变量


- dinov3
    - 使用 huggingface 的 transformers 库调用 dinov3 模型
    - ViT models pretrained：ViT-H+/16 distilled